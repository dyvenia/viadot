name: Notify Labeled DevOps

on:
  issues:
    types: [labeled]

jobs:
  notify-devops-label:
    uses: dyvenia/workflow-templates/.github/workflows/template_slack_notify.yml@main
    with:
      match_label: devops
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
