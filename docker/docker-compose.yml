version: "3"

services:
  viadot-lite:
    image: ghcr.io/dyvenia/viadot/viadot-lite:2.0.0-alpha.2
    container_name: viadot-lite
    volumes:
      # - ${HOME}/.databricks-connect:/home/viadot/.databricks-connect
      # - ${HOME}/.config/viadot/config.yaml:/home/viadot/.config/viadot/config.yaml
      - ../:/home/viadot
    shm_size: "4gb"
    command: sleep infinity
    restart: "unless-stopped"
  viadot-azure:
    image: ghcr.io/dyvenia/viadot/viadot-azure:2.0.0-alpha.2
    container_name: viadot-azure
    volumes:
      # - ${HOME}/.databricks-connect:/home/viadot/.databricks-connect
      # - ${HOME}/.config/viadot/config.yaml:/home/viadot/.config/viadot/config.yaml
      - ../:/home/viadot
    shm_size: "4gb"
    command: sleep infinity
    restart: "unless-stopped"
  viadot-aws:
    image: ghcr.io/dyvenia/viadot/viadot-aws:2.0.0-alpha.2
    container_name: viadot-aws
    volumes:
      # - ${HOME}/.databricks-connect:/home/viadot/.databricks-connect
      # - ${HOME}/.config/viadot/config.yaml:/home/viadot/.config/viadot/config.yaml
      - ../:/home/viadot
    shm_size: "4gb"
    command: sleep infinity
    restart: "unless-stopped"

